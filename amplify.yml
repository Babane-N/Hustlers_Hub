version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build -- --prod
  artifacts:
    baseDirectory: dist/Hustlers-Hub
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

